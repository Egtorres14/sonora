# Sonora funciona sin claves de API ni variables de entorno.
# Las claves externas se introducen únicamente en «Ver asistentes» → «Consultar una IA externa».
# Vite no incorpora claves de .env al bundle. No añadas variables VITE_* con secretos.
